# Сайт Татьяны Чаплыгиной

Одностраничный сайт психолога, сексолога и секс-коуча Татьяны Чаплыгиной (Москва, онлайн). Главное действие посетителя – написать в Telegram, чтобы записаться на консультацию. Акцент на мужскую аудиторию, женщины и пары – полноценные направления.

## Стек
Чистый HTML/CSS/JS, без сборщика и фреймворка.
- Плавный скролл: Lenis (`js/vendor/lenis.min.js`, взят из проекта EROTRAVERSE)
- Анимации: vanilla JS + IntersectionObserver
- Шрифт: Inter с оптическим размером (Google Fonts), в крупных кеглях работает как Inter Display

## Визуальный референс
https://kafaya.framer.website/ – разбор: `E:\2026 12 недель\Таня Чаплыгина\структура\03_Референс_KAFAYA.md`

## Запуск
Статический сайт, сборки нет. Локальный сервер из корня проекта:
```bash
python -m http.server 5178
```
Открыть http://localhost:5178/

## Структура
```
index.html        – главная страница
stati/<slug>/     – страницы статей (собираются скриптом, руками не править)
tools/build_stati.py   – сборка страниц статей и списка «Почитать» на главной
tools/stati-src/  – тексты статей, перенесённые с B17 (json)
sketches/         – наброски вариантов (первый экран и др.)
css/tokens.css    – цвета, шрифты, отступы
css/base.css      – сброс и базовые стили
css/article.css   – страница статьи
js/               – скрипты, js/vendor – сторонние библиотеки
assets/img/       – изображения
```

Статьи: правка текста – в `tools/stati-src/<id>.json`, новая статья – строка в `ARTICLES` в `tools/build_stati.py`, затем `python tools/build_stati.py`. Шапку, меню и подвал скрипт берёт из `index.html`, поэтому после их правки тоже перезапустить. При обновлении CSS/JS менять метку `?v=` в `index.html` до сборки.

## Материалы
- Структура и UX: `E:\2026 12 недель\Таня Чаплыгина\структура\`
- Материалы от клиентки: `E:\2026 12 недель\Таня Чаплыгина\материалы от Тани\`

## Ключи и переменные среды
Не нужны: сайт статический. Позже добавятся счётчик Яндекс.Метрики и ссылки на Telegram-бота – это публичные идентификаторы, не секреты.
